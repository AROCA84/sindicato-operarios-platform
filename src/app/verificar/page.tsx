'use client'; import {useState} from 'react'; import Header from '@/components/Header'; import Footer from '@/components/Footer'; export default function Verify(){const [code,setCode]=useState('');const [result,setResult]=useState<string>('');async function verify(e:React.FormEvent){e.preventDefault();setResult('Consultando…');try{const r=await fetch(`/api/certificados/verificar?code=${encodeURIComponent(code)}`);const d=await r.json();setResult(d.valid?`Certificado válido · emitido ${new Date(d.certificate.issued_at).toLocaleDateString('es-ES')}`:'No encontramos un certificado con ese código.')}catch{setResult('No se pudo completar la consulta.')}}return <><Header/><main className="container auth-page"><div className="auth-card"><span className="eyebrow">VERIFICACIÓN PÚBLICA</span><h1>Comprueba un<br/><span>certificado.</span></h1><p>Introduce el código único, por ejemplo SDO-XXXXXX.</p><form onSubmit={verify}><label>Código de certificado<input required value={code} onChange={e=>setCode(e.target.value.toUpperCase())} placeholder="SDO-XXXXXX"/></label><button className="btn btn-primary">Verificar código →</button></form>{result&&<div className="success-box">{result}</div>}</div></main><Footer/></>}
