'use client'; import Link from 'next/link'; import {useState} from 'react'; import Header from '@/components/Header'; import Footer from '@/components/Footer';
export default function Login(){const [submitted,setSubmitted]=useState(false); return <><Header/><main className="container auth-page"><div className="auth-card"><span className="eyebrow">BIENVENIDO DE NUEVO</span><h1>Accede a tu<br/><span>formación.</span></h1><p>Continúa donde lo dejaste.</p>{submitted?<div className="success-box">✓ Acceso preparado. Conecta Supabase para activar la autenticación real.</div>:<form onSubmit={e=>{e.preventDefault();setSubmitted(true)}}><label>Email<input required type="email" placeholder="tu@email.com"/></label><label>Contraseña<input required type="password" placeholder="Tu contraseña"/></label><button className="btn btn-primary" type="submit">Iniciar sesión →</button></form>}<p className="auth-switch">¿Todavía no tienes cuenta? <Link href="/registro">Regístrate gratis</Link></p></div></main><Footer/></>}
