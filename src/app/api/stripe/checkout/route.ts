import {NextResponse} from 'next/server'; import {supabase} from '@/lib/supabase';
export async function POST(request:Request){const secret=process.env.STRIPE_SECRET_KEY;if(!secret)return NextResponse.json({error:'Stripe no configurado. Añade STRIPE_SECRET_KEY.'},{status:503});if(!supabase)return NextResponse.json({error:'Supabase no configurado'},{status:503});const {courseId}=await request.json();const {data:{user}}=await supabase.auth.getUser();if(!user)return NextResponse.json({error:'Debes iniciar sesión'},{status:401});const {data:attempt}=await supabase.from('test_attempts').select('passed,score').eq('user_id',user.id).eq('course_id',courseId).eq('passed',true).order('created_at',{ascending:false}).limit(1).maybeSingle();if(!attempt)return NextResponse.json({error:'Debes aprobar el test antes de comprar el certificado'},{status:403});const {data:order,error}=await supabase.from('orders').insert({user_id:user.id,course_id:courseId,amount_cents:499,currency:'eur',status:'pending',provider:'stripe'}).select().single();if(error)return NextResponse.json({error:error.message},{status:500});return NextResponse.json({orderId:order.id,message:'Crear aquí la sesión oficial de Stripe con amount=499 y currency=eur.'});}
