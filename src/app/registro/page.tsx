'use client'; import Link from 'next/link'; import {useState} from 'react'; import Header from '@/components/Header'; import Footer from '@/components/Footer'; import {isSupabaseConfigured} from '@/lib/supabase'; import {signUp} from '@/lib/auth';
export default function Register(){const [form,setForm]=useState({name:'',email:'',password:''}); const [message,setMessage]=useState(''); const [loading,setLoading]=useState(false); async function submit(e:React.FormEvent){e.preventDefault();setLoading(true);setMessage(''); if(!isSupabaseConfigured){setMessage('La conexión de registro está preparada. Añade las variables de Supabase para activarla.');setLoading(false);return} const {error}=await signUp(form.name,form.email,form.password);setMessage(error?error.message:'Cuenta creada. Revisa tu email para confirmar el registro.');setLoading(false)} return <><Header/><main className="container auth-page"><div className="auth-card"><span className="eyebrow">ÚNETE A LA COMUNIDAD</span><h1>Empieza gratis.</h1><p>Estudia, haz los tests y decide después si quieres certificarte.</p><form onSubmit={submit}><label>Nombre completo<input required value={form.name} onChange={e=>setForm({...form,name:e.target.value})} placeholder="Tu nombre"/></label><label>Email<input required type="email" value={form.email} onChange={e=>setForm({...form,email:e.target.value})} placeholder="tu@email.com"/></label><label>Contraseña<input required minLength={8} type="password" value={form.password} onChange={e=>setForm({...form,password:e.target.value})} placeholder="Mínimo 8 caracteres"/></label><label className="check"><input type="checkbox" required/> Acepto los términos y la política de privacidad</label><button disabled={loading} className="btn btn-primary" type="submit">{loading?'Creando…':'Crear mi cuenta gratis →'}</button></form>{message&&<div className="success-box">{message}</div>}<p className="auth-switch">¿Ya tienes cuenta? <Link href="/login">Inicia sesión</Link></p></div></main><Footer/></>}
