import Link from 'next/link'; import Header from '@/components/Header'; import Footer from '@/components/Footer'; import {courses} from '@/lib/catalog';
export default function CoursesPage(){return <><Header/><main className="container page-main"><div className="page-hero"><span className="eyebrow">CATÁLOGO DE FORMACIÓN</span><h1>Aprende a trabajar<br/><span>mejor preparado.</span></h1><p>Todos los cursos incluyen temario y test gratuitos. Solo pagas si quieres tu certificado.</p></div><div className="course-grid">{courses.map(c=><Link href={`/cursos/${c.slug}`} className={`course-card ${c.accent}`} key={c.slug}><div className="course-art"><span className="art-symbol">{c.accent==='orange'?'▣':c.accent==='green'?'⬡':c.accent==='yellow'?'↟':'✚'}</span><span className="art-code">SDO / {c.slug.toUpperCase()}</span></div><div className="course-body"><span className="course-category">{c.category}</span><h3>{c.title}</h3><p>{c.description}</p><span className="course-link">Ver temario <b>→</b></span></div></Link>)}</div></main><Footer/></>}
